// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.huazhuo.shop.web;

import java.io.UnsupportedEncodingException;

import javax.servlet.http.HttpServletRequest;
import javax.validation.Valid;

import org.springframework.ui.Model;
import org.springframework.validation.BindingResult;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RequestMethod;
import org.springframework.web.bind.annotation.RequestParam;
import org.springframework.web.util.UriUtils;
import org.springframework.web.util.WebUtils;

import com.huazhuo.shop.city.Locality;
import com.huazhuo.shop.city.Provinces;

privileged aspect ProvincesController_Roo_Controller {
    
    @RequestMapping(method = RequestMethod.POST, produces = "text/html")
    public String ProvincesController.create(@Valid Provinces provinces, BindingResult bindingResult, Model uiModel, HttpServletRequest httpServletRequest) {
        if (bindingResult.hasErrors()) {
            populateEditForm(uiModel, provinces);
            return "provinceses/create";
        }
        uiModel.asMap().clear();
        provinces.persist();
        return "redirect:/provinceses/" + encodeUrlPathSegment(provinces.getId().toString(), httpServletRequest);
    }
    
    @RequestMapping(params = "form", produces = "text/html")
    public String ProvincesController.createForm(Model uiModel) {
        populateEditForm(uiModel, new Provinces());
        return "provinceses/create";
    }
    
    @RequestMapping(value = "/{id}", produces = "text/html")
    public String ProvincesController.show(@PathVariable("id") Integer id, Model uiModel) {
        uiModel.addAttribute("provinces", Provinces.findProvinces(id));
        uiModel.addAttribute("itemId", id);
        return "provinceses/show";
    }
    
    @RequestMapping(produces = "text/html")
    public String ProvincesController.list(@RequestParam(value = "page", required = false) Integer page, @RequestParam(value = "size", required = false) Integer size, Model uiModel) {
        if (page != null || size != null) {
            int sizeNo = size == null ? 10 : size.intValue();
            final int firstResult = page == null ? 0 : (page.intValue() - 1) * sizeNo;
            uiModel.addAttribute("provinceses", Provinces.findProvincesEntries(firstResult, sizeNo));
            float nrOfPages = (float) Provinces.countProvinceses() / sizeNo;
            uiModel.addAttribute("maxPages", (int) ((nrOfPages > (int) nrOfPages || nrOfPages == 0.0) ? nrOfPages + 1 : nrOfPages));
        } else {
            uiModel.addAttribute("provinceses", Provinces.findAllProvinceses());
        }
        return "provinceses/list";
    }
    
    @RequestMapping(method = RequestMethod.PUT, produces = "text/html")
    public String ProvincesController.update(@Valid Provinces provinces, BindingResult bindingResult, Model uiModel, HttpServletRequest httpServletRequest) {
        if (bindingResult.hasErrors()) {
            populateEditForm(uiModel, provinces);
            return "provinceses/update";
        }
        uiModel.asMap().clear();
        provinces.merge();
        return "redirect:/provinceses/" + encodeUrlPathSegment(provinces.getId().toString(), httpServletRequest);
    }
    
    @RequestMapping(value = "/{id}", params = "form", produces = "text/html")
    public String ProvincesController.updateForm(@PathVariable("id") Integer id, Model uiModel) {
        populateEditForm(uiModel, Provinces.findProvinces(id));
        return "provinceses/update";
    }
    
    @RequestMapping(value = "/{id}", method = RequestMethod.DELETE, produces = "text/html")
    public String ProvincesController.delete(@PathVariable("id") Integer id, @RequestParam(value = "page", required = false) Integer page, @RequestParam(value = "size", required = false) Integer size, Model uiModel) {
        Provinces provinces = Provinces.findProvinces(id);
        provinces.remove();
        uiModel.asMap().clear();
        uiModel.addAttribute("page", (page == null) ? "1" : page.toString());
        uiModel.addAttribute("size", (size == null) ? "10" : size.toString());
        return "redirect:/provinceses";
    }
    
    void ProvincesController.populateEditForm(Model uiModel, Provinces provinces) {
        uiModel.addAttribute("provinces", provinces);
        uiModel.addAttribute("localitys", Locality.findAllLocalitys());
    }
    
    String ProvincesController.encodeUrlPathSegment(String pathSegment, HttpServletRequest httpServletRequest) {
        String enc = httpServletRequest.getCharacterEncoding();
        if (enc == null) {
            enc = WebUtils.DEFAULT_CHARACTER_ENCODING;
        }
        try {
            pathSegment = UriUtils.encodePathSegment(pathSegment, enc);
        } catch (UnsupportedEncodingException uee) {}
        return pathSegment;
    }
    
}
